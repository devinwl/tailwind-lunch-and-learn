- When using Tailwind there are typically two paths forward when making components:

  - Using the utility classes.
  - Using the `@apply` keyword in CSS files.

- Both of these approaches can be mixed if desired.
