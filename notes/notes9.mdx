- Here is an example of our button using `@apply` to define its various classes.

- `@apply` is useful when specifying a repeating set of utility classes. A button could have its own `@apply` definition. Likely the most useful as components.
