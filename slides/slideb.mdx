## Responsive design

Tailwind supports responsive design by default using "variants".

There are preset variants for various screen sizes: `sm`, `md`, and `lg`.

They are used like this:

```jsx
<div className="w-full md:w-auto" />
```
