## Improving the developer experience

There are some tools available:

- `twin.macro` and `twind` -- change the syntax to resemble styled components:

```jsx
// twin.macro
import "twin.macro";

<div tw="border hover:border-black" />;

// twind
import tw from "twind";

<div className={tw`border hover:border-black`} />;
```
